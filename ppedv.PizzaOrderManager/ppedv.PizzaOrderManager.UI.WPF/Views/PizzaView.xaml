<UserControl x:Class="ppedv.PizzaOrderManager.UI.WPF.Views.PizzaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ppedv.PizzaOrderManager.UI.WPF.Views" 
             xmlns:viewmodels="clr-namespace:ppedv.PizzaOrderManager.UI.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800">
    <DockPanel>
        <WrapPanel DockPanel.Dock="Top" Margin="12">
            <Button Command="{Binding NewAddNewCommand}">Neu</Button>
            <Button Command="{Binding SaveCommand}">Speichern</Button>
            <Button>Löschen</Button>
        </WrapPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="2"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <DataGrid ItemsSource="{Binding PizzaList}"
                      SelectedItem="{Binding SelectedPizza}"/>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"/>

            <Grid Grid.Column="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Label>Name</Label>
                <TextBox Grid.Column="1" Text="{Binding SelectedPizza.Name}"/>
                <StackPanel Grid.Row="1">
                    <Label >Price</Label>
                    <TextBlock Text="{Binding PreisMitSteuer}"/>
                </StackPanel>
                <TextBox Text="{Binding SelectedPizza.Price}" Grid.Row="1" Grid.Column="2"/>
                <Label Grid.Row="2">Size</Label>
                <ComboBox Text="{Binding SelectedPizza.Size}" Grid.Row="2" Grid.Column="1"/>
                <GroupBox Header="Topping" Grid.Row="3" Grid.ColumnSpan="2">
                    <DataGrid ItemsSource="{Binding SelectedPizza.Toppings}"/>
                </GroupBox>
            </Grid>

        </Grid>
    </DockPanel>
</UserControl>
